# INTERLIS Web IDE

An offline-first, fully client-side INTERLIS IDE built around Monaco and the
shared `@ilic/language-service`. Workspaces live in OPFS or a user-selected
local folder; no REST backend is required.

```sh
corepack pnpm install
corepack pnpm check
```
